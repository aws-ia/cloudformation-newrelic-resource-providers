mutation {
  dashboardCreate(accountId: %d, dashboard: %s) {
    entityResult {
      guid
      name
      accountId
      createdAt
      updatedAt
    },
    errors {
      description
      type
    }
  }
}